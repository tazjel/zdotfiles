# Add Locale to Ubuntu Server 

sudo apt-get install language-pack-ar

# If language-pack-ar-base is not installed on your compter then the command 'dpkg -L language-pack-ar-base' will give followin error.

dpkg -L language-pack-ar-base
