# vim: set fileencoding=utf-8:
# لشرح الاوامر
vim cmd bash; explain your commands 

/\(.*\)/echo ">>> \1 >>> =$(\1)"/  

EXAMPLE :

count_home_dotfiles() { 
echo ">>> ls -1 .* | wc -l; >>> =$(ls -1 .* | wc -l;)"
echo ">>> ls -1 ./. | wc -l;  >>> =$(ls -1 ./. | wc -l; )"
echo ">>> ls -1 .* | wc -l; >>> =$(ls -1 .* | wc -l;)"
echo ">>> ls -1 * | wc -l; >>> =$(ls -1 * | wc -l;)"
echo ">>> ls -1 .* | wc -l; >>> =$(ls -1 .* | wc -l;)"
 }
